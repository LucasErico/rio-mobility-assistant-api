services:
  - type: web
    name: rio-mobility-assistant-api
    env: node
    buildCommand: npm install && npm run build
    startCommand: node dist/index.js
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: PORT
        value: 10000
